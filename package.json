{
  "name": "qrScan",
  "version": "1.2.1",
  "description": "",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --env-file=.env app.js",
    "generate": "node Scripts/generateDataFromSchemas.js",
    "omit-dev": "npm install --omit=dev",
    "prod": "npm run generate && npm run omit-dev && npm start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^2.2.0",
    "dotenv": "^17.4.2",
    "express": "^5.1.0",
    "lowdb": "^7.0.1"
  }
}